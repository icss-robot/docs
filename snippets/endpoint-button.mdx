export const EndpointButton = ({ title, href }) => (
    <div className="button-container">
        <a className="button" href={href}>
            {title}
            <span className="arrow">&#10140;</span>
        </a>
    </div>
);
